{
  "name": "fastify_typescript_template",
  "version": "1.0.0",
  "description": "Fastify is a web framework highly focused on providing the best developer experience with the least overhead and a powerful plugin architecture. It is inspired by Hapi and Express and as far as we know, it is one of the fastest web frameworks in town, With TypeScript that adds static typing with optional type annotations to JavaScript.",
  "main": "index.js",
  "scripts": {
    "build": "npm install && rimraf ./build && tsc",
    "dev": "nodemon",
    "start": "npm run build && node ./build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vishal Talukar",
  "license": "ISC",
  "dependencies": {
    "@fastify/cookie": "^9.1.0",
    "@fastify/cors": "^8.4.1",
    "@fastify/secure-session": "^7.1.0",
    "@fastify/swagger": "^8.12.0",
    "@fastify/swagger-ui": "^1.10.1",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "email-validator": "^2.0.4",
    "fastify": "^4.24.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^20.8.10",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1"
  }
}
